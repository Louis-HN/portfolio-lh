if (typeof initProfileTabs === 'function') initProfileTabs();
if (typeof renderMediaChart === 'function') renderMediaChart();
if (typeof renderDataChart === 'function') renderDataChart();
if (typeof renderCRMChart === 'function') renderCRMChart();
